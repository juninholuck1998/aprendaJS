// usa-se o + para concatenar

function apresentar (nome){
    let texto = "Olá," + nome
    return texto
}

// exemplo com texto e números

function calcularmedia (num1, num2, num3) {
    let media = (num1 * 3) + (num2 * 3) + (num3 * 4) / 10
    let resultado = "Sua média foi: " + media
    return resultado
}